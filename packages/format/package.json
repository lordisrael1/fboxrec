{
  "name": "@flightbox/format",
  "version": "0.0.0",
  "private": true,
  "description": "The .fbox incident format contract: types, parser, validator. Never published — bundled into `flightbox` via tsup noExternal and imported by the viewer via the workspace.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
