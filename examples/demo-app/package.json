{
  "name": "@flightbox/demo-app",
  "version": "0.0.0",
  "private": true,
  "description": "The victim: a normal-looking Express+Postgres app with one planted bug that melts the connection pool under load.",
  "scripts": {
    "db": "docker compose up -d",
    "seed": "node src/seed.js",
    "start": "node src/server.js",
    "loadtest": "node loadtest.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "fboxrec": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "autocannon": "^8.0.0"
  }
}
